# 2026-08-17

## ai-e2e-platform (main project, in-progress)

Built the full AI-powered E2E testing platform scaffold + working core
(LangChain/LangGraph/LangSmith/Playwright/FastAPI/Postgres/vector store).

- Repo: `ai-e2e-platform/` in workspace root.
- Full pipeline implemented as LangGraph state machine (13 nodes + conditional
  edges + retry/heal budgets + human-approval resume graph).
- Verified this session:
  - Created `.venv` (Python 3.13 + `--system-site-packages`); Python 3.14 is
    too new for some wheels (asyncpg/pydantic-core).
  - `pytest tests -q` → **23 passed**.
  - FastAPI app imports; all 6 `/api/v1/*` routes return 200 via TestClient;
    OpenAPI enumerates them.
  - Both LangGraph graphs compile (main: 13 nodes; approval: 11 nodes).
- Bugs found + fixed this session:
  1. LangChain prompt templates crashed on literal `{name, steps[]}` and
     `{action, target, value, expected}` — escaped to `{{...}}` in
     `agents/requirements.py` and `agents/generator.py`.
  2. Prioritiser test over-asserted (history factor weight 0.15 can't push a
     low-risk test to P1) — fixed test to assert score increase instead.
- Env note: tool output redacts password-like values (e.g. `POSTGRES_PASSWORD`
  shows `***`); actual file content is correct. Verify with hexdump if unsure.

### Remaining (blocked on user's OpenAI key / live target)
- Live LLM inference + browser execution (need `OPENAI_API_KEY`, a target URL).
- Production hardening: JWT RBAC (currently dev-token), secrets vault, worker
  queue (runs are synchronous), Alembic migrations (currently `create_all`).

## 2026-08-17 (session 2) — live verification + OpenRouter

- OpenRouter key stored in gitignored `.env` (provider=openrouter).
- Free-model landscape changed: `meta-llama/...:free` gone. Verified default =
  `google/gemma-4-31b-it:free` (function calling works; complex schemas
  sometimes return markdown, and shared pool 429s intermittently).
  `nvidia/nemotron-3-super-120b-a12b:free` = chat only (no tool calls).
- Pinned `playwright==1.61.0` — 1.62+ needs macOS 14+ for chromium; 1.61 reuses
  the cached chromium-1228 build (works on this mac13 host).
- Added deterministic fallbacks so the pipeline works offline/rate-limited:
  `fallback_generate_tests`, `heuristic_classify`, `heuristic_heal`.
- Fixed: executor step results now include `target` (healing couldn't find the
  broken locator before); discovery crawler JS; `.env` path (cwd-agnostic).
- Live run vs saucedemo.com: 3/3 pass. Failing test → product_defect.
  Broken locator → automation_defect → healed → re-passed. **31 tests pass.**

## 2026-08-17 (session 3) — durable persistence

- Added `services/persistence.py`: runs/results/failures/healing_events now
  persist via the async ORM. Dev default = SQLite (`.env` DATABASE_URL), prod
  = Postgres (`docker compose up`). API `/runs/{id}` + `/dashboard/summary`
  read from DB (in-memory fallback). `report_node` persists at end of run.
- Fixed duplicate index on `TestCase` (index=True + explicit Index).
- `aiosqlite` added. **34 tests pass.**

## 2026-08-17 (session 4) — JWT RBAC

- `security.create_token/decode_token` (PyJWT HS256); `deps.get_principal` now
  decodes JWTs (role → Principal) in addition to the admin token; CLI
  `python -m app.cli token --role engineer` mints tokens. `SECRET_KEY` must be
  >=32 bytes (set in `.env`). **39 tests pass.**

## 2026-08-17 (session 5) — Gemini provider + async worker queue

- Tested the LLM path live: free OpenRouter model fails 3 ways (markdown
  fences, bare-list/wrong-key JSON, 429). Added `llm.structured_invoke`
  (fence-strip → list-wrap → key-coerce → strict-JSON retry) across all 5
  agents. Selector fallback widened to text/aria-label/placeholder/data-testid.
- Switched LLM to **Google Gemini** via its OpenAI-compatible endpoint
  (`gemini-3.6-flash`; `gemini-2.5-flash` retired for new keys). Gemini now
  generates real coverage (happy/negative/boundary/navigation/XSS). Fixed
  executor `_goto` to accept URL in `value` (Gemini's step format).
- Added `services/worker.py` + `services/run_store.py`: `POST /runs` is now
  non-blocking (returns `queued` in ~77ms); worker drains the queue
  (`queued→running→passed`). `persist_run` made idempotent; added
  `persist_run_pending`/`update_run_status`. **49 tests pass.**

## 2026-08-17 (session 6) — Alembic migrations (platform complete)

- `alembic>=1.13`; `backend/alembic.ini` + `backend/alembic/` (async env.py
  reading `settings.database_url` via `_resolve_db_url`). Initial autogenerated
  migration `6691d0bbc5bf_initial_schema` (13 create_table) verified:
  `upgrade head` + `downgrade base` round-trip on a fresh SQLite.
- `main.py` lifespan: prod/staging runs `alembic upgrade head` (via
  `asyncio.to_thread`); dev/test keep `create_all`. Makefile `migrate`/
  `migration` targets. **49 tests pass; all spec items now implemented.**

## 2026-08-17 (session 7) — free LLM decision

- Gemini free tier = 20 req/day (exhausted during acceptance run → degraded
  tests). Switched back to **OpenRouter** free model
  `google/gemma-4-26b-a4b-it:free` (TOOLS+STRUCT, verified: 4 domain-aware
  tests — happy/negative/boundary/security — through the JSON hardening).
  `nemotron-3-super-120b-a12b:free` hung/timeout; `openrouter/free` untested.
  GEMINI_API_KEY kept in `.env` for later.
